import 'package:flutter/widgets.dart';

class DetailGoalsPage extends StatelessWidget {
  const DetailGoalsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}